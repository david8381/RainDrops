#!/bin/sh
set -e

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
"$ROOT_DIR/scripts/stamp-version.sh"
node "$ROOT_DIR/scripts/gen-code-map.mjs" >/dev/null

git add "$ROOT_DIR/index.html" "$ROOT_DIR/package.json" "$ROOT_DIR/docs/Ai/CODE_MAP.md"
